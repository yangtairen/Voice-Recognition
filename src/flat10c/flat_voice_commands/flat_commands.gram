#JSGF V1.0;

/**
 * JSGF Grammar for Flat Voice Commands
 */

grammar flat_commands;

public <power_control> = (Turn on | Turn off);
public <hello> = (Hello | Hi | Good (Morning | Evening | Afternoon));
public <movement> = (Up | Down | Right | Left);
